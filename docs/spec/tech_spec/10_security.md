# Безопасность

---

## Правила

| Правило | Как реализовано |
|---|---|
| SQL-инъекции | Только параметризованные запросы через `pg` |
| XSS (кража токенов) | JWT хранится в `httpOnly cookie` — JavaScript не имеет доступа |
| Несанкционированный доступ к админке | Все `/api/admin/*` проверяют JWT с ролью `admin` |
| Дублирование заказов (Stripe webhook) | `UNIQUE` на `stripe_payment_id` + проверка перед созданием |
| Поддельные Stripe webhook | Проверка подписи через `stripe.webhooks.constructEvent()` |
| Чужие заказы | `GET /api/orders/:id` требует `tracking_token` или авторизованную сессию |
| CORS | Настроен только на домен ресторана (`oumiroll.fr`) |
| Пароли в открытом виде | Никогда — только bcrypt-хэш (salt rounds: 12) |
| Перебор паролей | Rate limiting + блокировка по БД после 5 попыток |
| Спам SMS | Rate limiting: 3 запроса / 15 минут на номер телефона |
| Утечка API-ключей | Все секреты в Vercel Environment Variables, не в коде |

---

## Доступ к данным заказа

Страница `/suivi/[id]` требует одно из двух:
- Валидный `?token=<tracking_token>` в URL
- Авторизованную сессию владельца этого заказа

Без одного из двух — сервер возвращает `403`.

---

## Cookie-настройки

```
httpOnly: true      ← JavaScript не видит токен
Secure: true        ← только HTTPS
SameSite: Strict    ← защита от CSRF
```

---

## Что проверяется перед каждым запросом к admin API

1. Наличие JWT в cookie
2. Валидность подписи токена
3. Роль = `admin`

Если хотя бы одно условие не выполнено → `401` или `403`.
